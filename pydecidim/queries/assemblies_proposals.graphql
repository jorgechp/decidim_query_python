#This query retrieves ids of assemblies.

{
    assembly(id:"%ID_ASSEMBLY%") {
        id
        components(filter:{type: "Proposals" }){
            id
            ... on Proposals{
                proposals{
                    nodes{
                        id
                    }
                }
            }
        }
    }
}